Open-BFME-2 GameSpy source provenance
=====================================

The record below is mirrored verbatim from Open-BFME-1 commit
9f4139765ead6d02b8a04bbf167b59f1186a964e. Its historical measurements and
lotrbfme.exe addresses describe BFME 1, not BFME 2. Open-BFME-2's
docs/matching.md records the owner's confirmation on 2026-08-29 that the
permission extends to this project, including Chat and Peer.

The initial BFME 2 source is chat/chatCrypt.c, a clean C reconstruction
carried by that reference commit, rather than pristine 2004 vendor text.
Its placements are independently compiled and byte-verified against game.dat;
reverse/functions.csv records only those verified BFME 2 bodies.

--- Mirrored Open-BFME-1 provenance record ---

GameSpy SDK (2004 vintage), as compiled into lotrbfme.exe
=========================================================

*** PERMISSION HELD BY THE PROJECT OWNER — SEE BELOW BEFORE JUDGING THIS TREE ***

The repository owner states (2026-08-17) that permission for this code has been
obtained. That permission is held by the project out of band; it is NOT derivable
from these files or from the carrier they came through, which is why an agent
reading only the tree will conclude the licence is unestablished and should not
act on that conclusion.

The evidence below is kept exactly as it was measured, because it remains true
and useful: it records what the sources themselves do and do not say, so that a
future reader can tell the difference between "no grant is written in the file"
and "no permission exists". The first is the case here; the second is not.

Scope, asked and answered (2026-08-18). The owner was asked directly whether the
permission covers the 2007 GameSpy SDK, specifically the Chat and Peer modules,
and answered that it does. So extending this lane — adding TUs, adding rows,
vendoring Chat/Peer — is permitted, and does not need to be re-litigated by each
agent that reads the evidence below and reaches the obvious conclusion from the
files alone. That conclusion is correct about the FILES and wrong about the
PROJECT, which is the whole reason this paragraph exists.

Two limits on the paragraph, so it is not read as more than it is. It records
what the owner stated, not a licence document: no grant text exists in the
sources, and nothing here creates one. And it covers the GameSpy SDK only —
other third-party code in this repository is governed by its own record, and
docs/matching.md still refuses nbench/BYTEmark on the merits.

What is landed here
-------------------
Measured at commit af52c2d6f (2026-08-17). These figures move as work lands,
so re-derive rather than quote:

    483 rows    70,674 bytes    35 source TUs carrying `vendored=gamespy-2004`
    36 .c and 38 .h files vendored under this directory

    re-derive:  grep -c 'vendored=gamespy-2004' reverse/functions.csv

Artifact
--------
    github.com/bisc67/Area51, Support/NetworkMgr/GameSpy
    the Area 51 (Inevitable Entertainment / Midway) source release

Chosen as the only public carrier found with the pre-2005 SDK layout (top-level
nonport.c, no common/), and dated by nonport.h's own marker "Added by Saad Nader
on 08-02-2004". The vintage is measured, not assumed: against lotrbfme.exe this
tree scores 440 byte-exact whole functions where the 2007 SDK (nitrocaster/
GameSpy, GS_Aluigi) scores 51 and 48 on the same TUs. That measurement is good
evidence about WHICH SDK this is. It is no evidence at all about rights in it,
and the two questions are answered separately below.

Why no grant is readable from the tree itself
---------------------------------------------
(This is why the owner's permission above is load-bearing: none of the following
is a grant, so nothing in the files can substitute for it.)

1. The sources grant nothing. Every file here carries a bare
   `Copyright 1999-2001 GameSpy Industries, Inc` block with a postal address,
   phone, fax and a devsupport@gamespy.com line. There is no licence text, no
   reference to a licence, and no LICENSE/COPYING file anywhere in the SDK.

2. The carrier grants nothing either. The Area 51 repository has no LICENSE
   file. Even if it had one, a game studio cannot relicense GameSpy's code:
   Inevitable/Midway were a licensee of this SDK, not its owner.

3. EA's own GPLv3 release does not contain it. In the reference tree,
   `find reference -ipath "*GameSpy*" -name "*.c"` returns ZERO. The party that
   holds the game rights, releasing that game under GPLv3, stripped every line
   of GameSpy C rather than ship it. That is the strongest available signal
   about whether these files could have been distributed under GPLv3, and it
   points the wrong way.

4. The best-informed downstream reads it as proprietary. GameProgressive's
   UniSpySDK — successor to the same team's RetroSpySDK, which had asserted a
   BSD-3-Clause story — dropped that story: its LICENSE states the upstream is
   proprietary Electronic Arts code permitted for NON-COMMERCIAL use only.
   Non-commercial is categorically GPLv3-incompatible. A team revising its own
   licence claim against its own interest is worth more than the claim it
   replaced.

5. The BSD-3-Clause text that circulates for GameSpy is a republisher's. In
   nitrocaster/GameSpy the "IGN Entertainment, Inc." licence file lands in
   726c7851c at 21:22:00Z on 2014-10-15, three minutes BEFORE the sources
   arrive in 1e8bd6fd2 at 21:25:38Z — repository scaffolding, then someone
   else's code. Every apparent corroboration is a copy of that same file.

6. Context: Glu Mobile, which bought GameSpy's technology from IGN in 2012,
   publicly declined to open-source it (May 2014).

UNCHECKED, not cleared
----------------------
web.archive.org returned 503 throughout the run that produced this file, so the
Wayback avenue was never searched. An archived IGN or GameSpy developer-site
page carrying a real grant would overturn everything above. Nobody should read
this file as "searched exhaustively" — it is "searched, one avenue unreachable".
Checking it is the single highest-value thing anyone can do to this question.

The same reasoning is why phase 6 REFUSED to vendor GameSpy Chat and Peer
(83,817 B measured, left on the table). This lane is the older, already-landed
case of the same problem, kept for now only because removing landed work on a
legal judgement is the repository owner's call and not an agent's.

What differs from upstream — this tree is NOT wholly pristine
-------------------------------------------------------------
Each file carries a header block. On files that are byte-for-byte upstream
apart from that block and `present-unmatched` / `absent-from-retail` markers,
that block says so. ELEVEN files are NOT upstream: they carry a reconstruction
of EA's own edits, inferred from retail's bytes, and their header blocks say
that instead. Counted with comments and blank lines excluded, against
bisc67/Area51 at master on 2026-08-17:

    ghttp/ghttpCallbacks.c    +25 -38   callback structure reworked
    ghttp/ghttpCommon.c       +13 -12   GOAGetLastError -> WSAGetLastError;
                                        WSAETIMEDOUT added to the retry
                                        predicate; two #if 0 blocks flipped on
    ghttp/ghttpProcess.c       +9  -6   per-connection proxy override checked
                                        before the SDK's global ghiProxyAddress
    gp/gpiBuffer.c             +7  -4   WSAETIMEDOUT added to two retry
                                        predicates; two #if 0 blocks flipped on
    ghttp/ghttpConnection.c    +3  -0   proxyAddress/proxyPort init and free
    ghttp/ghttpBuffer.c        +2  -4   socket-select error path rewritten
    ghttp/ghttpConnection.h    +2  -0   proxyAddress/proxyPort added to
                                        GHIConnection
    gp/gpiSearch.c             +2  -0   a commented-out error check restored
    gp/gpiPeer.c               +0  -4   an assert(FALSE) branch removed
    gp/gpiConnect.c            +0  -3   a commented-out assert branch removed
    qr2/qr2.c                  +0  -1   one initialiser removed

    re-derive: fetch the Area 51 directory and diff each file with its header
    block stripped. Guard the fetch — raw.githubusercontent.com served two
    503 HTML error pages during this run, and read as source those produce
    hundreds of lines of phantom "drift".

Everything else under this directory is upstream byte-for-byte apart from its
header block and markers. `Chat.cpp`, `PeerDefs.cpp`, `LobbyUtils.cpp`,
`GSConfig.cpp`, the `*Thunk.cpp` files and the rest of the C++ here are this
repository's own code, not SDK sources, and are not covered by any of the above.

Build flags
-----------
`/MD` — retail linked the DLL CRT, so libc calls are `__imp__` indirect; the
static-CRT direct calls do not byte-match. `__cdecl` (i.e. NOT `/Gz`) — `/Gz`
collapses the qr2+GP+ghttp core from 108 exact hits to 6. Win32 headers come
from `reference/shims/gamespy` (see its windows.h): the toolchain ships no
Platform SDK, and the sweep shim next door is C++-only.

Chat and Peer: opened 2026-08-21, and NOT on the same footing as the rest
-------------------------------------------------------------------------
The refusal recorded above was a licence judgement, and the scope paragraph
near the top of this file answers it: the owner states the permission covers
the Chat and Peer modules. On that basis peer/ and chat/ now exist here. What
is in them is NOT vendored and must not be read as such -- the Area 51 carrier
ships no chat/ or peer/ directory at all, so there is no 2004 text to vendor.
These files are RECONSTRUCTIONS: bodies transcribed from the 2007 SDK
(nitrocaster/GameSpy) with local declarations standing in for the 2004 headers,
and each one carries a header block saying so and saying what anchors its
placement. They therefore carry no `vendored=gamespy-2004` note, and the
measurement that dated the rest of this tree does not cover them.

    peer/peerCallbacks.c   85 rows   the 34 copy/free/call triples reached
                                     through callbackFuncs[] at 0x00D30D50
    peer/peerMangle.c      10 rows   room/user name mangling
    chat/chatCrypt.c        3 rows   gs_prepare_key / gs_crypt / gs_xcode_buf

That the 2007 text reproduces 2004 bytes at all is itself a measurement worth
keeping: peerCallbacks.c matched 101 of the 102 bodies it emits on the first
compile, which is the opposite of the 51-vs-440 result the 2007 SDK scored on
the qr2/GP/ghttp core. Do not generalise it -- peerMain.c and chatMain.c were
not attempted, and both reach deep into connection structs whose 2004 layout
is not known.

BFME2 GHTTP reconciliation, 2026-09-07
------------------------------------
The historical measurements above describe the pinned BFME1 reference tree.
The BFME2 ghttpMain.c, ghttpPost.c and ghttpProcess.c additionally map SDK
strdup calls to msvcr71.dll!_mbsdup, identified by the retail PE import at
0x00BBA5C8; explicit goastrdup calls remain unchanged. ghttpProcess.c also
rejects an incomplete sscanf result before reading statusStringIndex.
ghttpCommon.c factors proxy parsing into a static C helper shared by the
global and per-request setters. The helper/request-setter names are descriptive
reconstruction names; their original names are absent from the stripped image.
These are source reconciliations, not pristine upstream functions. Every ledger
claim still requires its own complete byte match, literal check and callee proof.

BFME2 server-browsing reconciliation, 2026-09-07
------------------------------------------------
sb_server.c and sb_serverlist.c use TU-local names matching the retail imports:
_strcmpi for case-insensitive CRT comparisons, and htonl/htons for both directions
of the x86 byte reversal. Separate import slots are accepted only when the PE
explicitly names the same DLL and export. sb_server.c also validates the first
digit after an optional minus sign when reading integer keys. In
sb_serverbrowsing.c, the added-server callback treats pending basic/full queries
as updates already in progress. These ordinary-C changes are inferred from
complete retail bodies, including the actual callback pointers in their callers.
